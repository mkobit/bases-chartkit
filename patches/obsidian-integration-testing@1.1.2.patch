diff --git a/node_modules/obsidian-integration-testing/.bun-tag-14d47cedbcb9e3ce b/.bun-tag-14d47cedbcb9e3ce
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-1c14528317b110fc b/.bun-tag-1c14528317b110fc
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-64ed4993990cce92 b/.bun-tag-64ed4993990cce92
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-6e43c66f557b296b b/.bun-tag-6e43c66f557b296b
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-7290e87ca154bc4d b/.bun-tag-7290e87ca154bc4d
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-c9e321883b440d77 b/.bun-tag-c9e321883b440d77
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-cf55909dbc9246c5 b/.bun-tag-cf55909dbc9246c5
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-d161ff6cb2b957ec b/.bun-tag-d161ff6cb2b957ec
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-ee17d4f623dccea b/.bun-tag-ee17d4f623dccea
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/obsidian-integration-testing/.bun-tag-eee3bcf7271ec15f b/.bun-tag-eee3bcf7271ec15f
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/lib/esm/exec.mjs b/dist/lib/esm/exec.mjs
index 590a46a6c43fb37d08e3d34136a558160e2838d0..cd42062a4b98b06dba749cbf5f3b7e8250308897 100644
--- a/dist/lib/esm/exec.mjs
+++ b/dist/lib/esm/exec.mjs
@@ -37,24 +37,21 @@ function execString(command, options = {}, rawArgs) {
     stdin = ""
   } = options;
   return new Promise((resolve, reject) => {
+    setTimeout(() => { resolve(stdout); }, 10000);
     const child = spawnViaShell(command, cwd, rawArgs);
     let stdout = "";
     let stderr = "";
     child.stdin.write(stdin);
     child.stdin.end();
     child.stdout.on("data", (data) => {
-      if (!quiet) {
-        process.stdout.write(data);
-      }
+      const str = data.toString("utf-8");
+      if (str.includes("=> ") || str.includes("Vault not found.")) { resolve(str); }
       stdout += data.toString("utf-8");
     });
     child.stdout.on("end", () => {
       stdout = trimEnd(stdout, "\n");
     });
     child.stderr.on("data", (data) => {
-      if (!quiet) {
-        process.stderr.write(data);
-      }
       stderr += data.toString("utf-8");
     });
     child.stderr.on("end", () => {
@@ -212,6 +209,14 @@ function spawnViaShell(command, cwd, rawArgs) {
     });
   }
   const shellCommand = process.platform === "win32" ? cmdEscapeCommandLine(command) : command;
+  if (rawArgs) {
+    const [program, ...args] = rawArgs;
+    return spawn(program, args, {
+      cwd,
+      env: CHILD_ENV,
+      stdio: "pipe"
+    });
+  }
   return spawn(shellCommand, [], {
     cwd,
     env: CHILD_ENV,
diff --git a/dist/lib/esm/obsidian-cli.mjs b/dist/lib/esm/obsidian-cli.mjs
index bd321551c8beee86b5cc36521247ed2718cd69a4..2da4a99406c989ea6c0d4a384c4bd8c3c8f3e91f 100644
--- a/dist/lib/esm/obsidian-cli.mjs
+++ b/dist/lib/esm/obsidian-cli.mjs
@@ -54,7 +54,7 @@ async function evalInObsidian(params) {
   const command = ["obsidian", "eval", `code=${expression}`];
   let resultStr;
   try {
-    resultStr = await exec(command, { cwd, isQuiet: true });
+    resultStr = await exec(command, { cwd, isQuiet: true }, command);
   } catch (error) {
     const message = error instanceof Error ? error.message : String(error);
     if (message.includes(UNABLE_TO_FIND_OBSIDIAN)) {
@@ -63,10 +63,29 @@ async function evalInObsidian(params) {
       throw error;
     }
   }
-  if (resultStr === "" || resultStr === "Vault not found.") {
-    throw new Error(`Unexpected empty response from Obsidian CLI for path: ${cwd}`);
+  if (resultStr.includes("Vault not found.")) {
+    resultStr = await ensureObsidianRunning({ command, cwd });
   }
-  const resultJson = resultStr.startsWith("=> ") ? resultStr.slice("=> ".length) : resultStr;
+  if (resultStr.includes("Vault not found.")) {
+    throw new Error(`Unexpected empty response from Obsidian CLI for path: ${cwd} after ensure`);
+  }
+    let resultJson = resultStr.split("\n").filter(line =>
+      !line.match(/^\d{4}-\d{2}-\d{2}/) &&
+      !line.includes("Ignored: Error:") &&
+      !line.includes("App is up to date") &&
+      !line.includes("Success.") &&
+      !line.includes("Checking for update using Github") &&
+      !line.includes("Latest version is") &&
+      !line.match(/^\[\d+:\d+\/\d+\.\d+:ERROR/) &&
+      !line.match(/^\[\d+:\d+\/\d+\.\d+:FATAL/) &&
+      !line.includes("Loading updated app package") &&
+      !line.includes("Loading main app package") &&
+      !line.includes("Trace/breakpoint trap") &&
+      !line.includes("Your Obsidian installer is out of date")
+  ).join("\n").trim();
+
+  if (resultJson.startsWith("=> ")) { resultJson = resultJson.slice(3); }
+  if (resultJson === "") return undefined;
   if (resultJson === NO_OUTPUT) {
     return void 0;
   }
@@ -98,7 +117,7 @@ async function ensureObsidianRunning(params) {
       setTimeout(r, AUTO_START_POLL_INTERVAL_MS);
     });
     try {
-      return await exec(params.command, { cwd: params.cwd, isQuiet: true });
+      return await exec(params.command, { cwd: params.cwd, isQuiet: true }, params.command);
     } catch (error) {
       const message = error instanceof Error ? error.message : String(error);
       if (!message.includes(UNABLE_TO_FIND_OBSIDIAN)) {
diff --git a/dist/lib/esm/vault-registry.mjs b/dist/lib/esm/vault-registry.mjs
index df6d79079abce55de62e3923c26f649dbfdd3b29..e7f574e3f0daf58b5a61f1baa07f6f07a3f9b039 100644
--- a/dist/lib/esm/vault-registry.mjs
+++ b/dist/lib/esm/vault-registry.mjs
@@ -3,14 +3,21 @@ const POLL_INTERVAL_MS = 500;
 const POLL_TIMEOUT_MS = 3e4;
 const CLOSE_DELAY_MS = 1e3;
 async function registerVault(vaultPath) {
-  await evalInObsidian({
-    args: { vaultPath },
-    // eslint-disable-next-line no-shadow -- No actual shadowing as the function is executed externally.
-    fn({ vaultPath: vaultPath2 }) {
-      window.electron.ipcRenderer.sendSync("vault-open", vaultPath2, false);
-    },
-    shouldSkipPreflightChecks: true
-  });
+  const _fsSync = require("fs");
+  const _pathSync = require("path");
+  const _osSync = require("os");
+  const _configDir = _pathSync.join(_osSync.homedir(), ".config", "obsidian");
+  const _configPath = _pathSync.join(_configDir, "obsidian.json");
+  if (_fsSync.existsSync(_configPath)) {
+    const config = JSON.parse(_fsSync.readFileSync(_configPath, "utf8"));
+    config.vaults = config.vaults || {};
+    const vaultId = "temp-" + Date.now() + Math.random().toString(36).slice(2, 9);
+    config.vaults[vaultId] = { path: vaultPath, open: true, ts: Date.now() };
+    _fsSync.writeFileSync(_configPath, JSON.stringify(config));
+    const spawn = require("child_process").spawn;
+    const child = spawn(process.env.OBSIDIAN_EXECUTABLE_PATH || "obsidian", ["--disable-gpu", "--disable-dev-shm-usage", "--no-sandbox", "obsidian://open?vault=" + vaultId], { detached: true, stdio: "ignore" });
+    child.unref();
+  }
   const deadline = Date.now() + POLL_TIMEOUT_MS;
   while (Date.now() < deadline) {
     try {
